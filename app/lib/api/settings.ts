export const API_URL = "http://10.0.2.2:8080/api"; 
// We use this IP address to point to the local machine, instead of localhost (when running on Android)
// Change this to the IP address of your local machine, if you want to run the app on your phone

console.info("API URL:", API_URL);